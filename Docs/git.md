# Git command

## Git submodule

Add a submodule
```bash
git submodule add https://github.com/STMicroelectronics/STM32CubeG4.git TARGET/STM32CubeG4
# Push and then
git submodule update --init --recursive
```